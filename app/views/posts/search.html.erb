    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
           <h1><%= link_to "〜Allage〜", "/"%></h1>
          </div>

          <%if user_signed_in? %>
            <div class="nav__right"> 
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "nav__btn"%> 
              <%= link_to "Allager",new_post_path, class: "nav__btn" %>
              
            </div>
          <% else %>
            <div class="nav__right">
              <%= link_to "ログイン",  new_user_session_path, class: "nav__btn" %>
              <%= link_to "新規登録", new_user_registration_path, class: "nav__btn" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>




<div class="card_title">
  <h2><span class="fas fa-crown"></span>人気コーデランキング<span class="fas fa-crown"></span></h2>
</div>
<% @posts.each do |post|%>
  <div class="card">
    <div class="se_photo">
     <%= link_to image_tag(post.image, class: 'se_img' ), post_path(post.id)%>
     <%= link_to %>
      <p>1</p>
   </div>
  <% end %>
  </div>
<%= render partial: 'shared/footer'%>
